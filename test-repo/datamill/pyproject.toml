[project]
name = "datamill"
version = "0.2.0"
description = "A CLI tool for processing and transforming CSV/JSON datasets"
requires-python = ">=3.10"
dependencies = [
    "click>=8.0.0",
    "pandas>=2.0.0",
    "pyyaml>=6.0",
    "requests>=2.31.0",
]

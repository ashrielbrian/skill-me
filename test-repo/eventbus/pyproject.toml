[project]
name = "eventbus"
version = "1.3.0"
description = "In-process event bus with persistent storage and middleware support"
requires-python = ">=3.11"
dependencies = ["aiosqlite>=0.19.0", "pydantic>=2.0.0"]

[project.optional-dependencies]
dev = ["pytest>=7.0.0", "pytest-asyncio>=0.21.0"]
